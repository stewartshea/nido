apiVersion: v1
kind: Service
metadata:
  name: nido
  namespace: nido
spec:
  selector:
    app: nido
  ports:
    - name: web
      port: 80
      targetPort: 3001
    - name: api
      port: 3000
      targetPort: 3000